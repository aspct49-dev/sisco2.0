# Copy to .env.local for dev. In production, set these in the Vercel
# dashboard (Project → Settings → Environment Variables). Never commit a
# file with real secrets.

# Shuffle needs no key — the public affiliate ID for code SISCO is baked into
# api/_lib/leaderboard.js. Only set this if the ID ever changes.
# SHUFFLE_AFFILIATE_ID=
